{
  "name": "a2a-web",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:dashboard": "pnpm --filter a2a-dashboard dev",
    "dev:docs": "pnpm --filter a2a-docs dev"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.5.3"
  }
}
